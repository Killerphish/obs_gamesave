/* Stub for deprecated AGL framework (removed from recent macOS SDKs).
   Qt's macOS config still links -framework AGL; this provides an empty dylib so the link succeeds. */
void agl_stub_placeholder(void) {}
